To ensure system security and stability, the operating system was updated and upgraded using
terminal commands. Updating synchronizes the system with official repositories, while
upgrading installs the latest versions of software packages.
